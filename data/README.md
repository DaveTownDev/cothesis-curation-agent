# data/ — your exports go here

These files are provided by Dave (not generated by Claude Code). Drop them in, then Day 1–2 of the build loads them.

- `resources_seed/` — the resources to enrich + index (see its README).
- `methodologies/` — the four MVP methodology definitions (see its README).
- `editorial_examples/` — the editorial voice anchor (already included).
