rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access is server-side via Admin SDK / runtime SA — deny client SDKs.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
